extends layout

block content
	ul.entries
		for entry in entries
			li
				span.name #{entry.name}
				span.surname #{entry.surname}
				span.number #{entry.number}
				a(href='/edit/#{entry._id}', class='icon edit')
				a(href='/delete/#{entry._id}', class='icon delete')
		else
			li no entries yet!